pipeline:
  release:
    image: plugins/docker
    settings:
      repo: "${CI_REPO_OWNER}/${CI_REPO_NAME}"
      tags: "${CI_REPO_TAG##v}" # strips v from the tag
    when:
      event: tag
      tag: v*
